kind: Service
apiVersion: v1
metadata:
  name: dock-management-service
  labels:
    app: dock-management-service
spec:
  selector:
    app: dock-management-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8900

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dock-management-service
  labels:
    app: dock-management-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dock-management-service
  template:
    metadata:
      labels:
        app: dock-management-service
    spec:
      containers:
        - name: dock-management-service
          image: dock-management-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8900
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
#      imagePullSecrets: 
#      - name: gitlab-dock-management
