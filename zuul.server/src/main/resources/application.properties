
server.port=8762
spring.application.name=zuul-server
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.instance.hostname=localhost
eureka.client.registerWithEureka=true
eureka.client.fetchRegistry=true

# A prefix that can added to beginning of all requests. 
#zuul.prefix=/api

# Disable accessing services using service name (i.e. payment-service).
# They should be only accessed through the path defined below.
zuul.ignored-services=*

# Map paths to services
zuul.routes.account-service.path=/account/**
zuul.routes.account-service.serviceId=account-service

# Map path to auth service
zuul.routes.auth-service.path=/auth/**
zuul.routes.auth-service.serviceId=auth-service

# Map path to dock service
zuul.routes.dock-service.path=/dock/**
zuul.routes.dock-service.serviceId=dock-service

# By default, all requests to payment service for example will start with: "/payment/"
# What will be sent to the payment service is what comes after the path defined, 
# So, if request is "/payment/view/1", payment service will get "/view/1".
# In case of auth, we need to pass the "/auth/" in the path to auth service. So, set strip-prefix to false
zuul.routes.auth-service.strip-prefix=false

# Exclude authorization from sensitive headers
zuul.routes.auth-service.sensitive-headers=Cookie,Set-Cookie   

ribbon.ReadTimeout=60000