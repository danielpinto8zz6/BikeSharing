kind: Service
apiVersion: v1
metadata:
  name: payment-manager-service
  labels:
    app: payment-manager-service
spec:
  selector:
    app: payment-manager-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 9100

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-manager-service
  labels:
    app: payment-manager-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-manager-service
  template:
    metadata:
      labels:
        app: payment-manager-service
    spec:
      containers:
        - name: payment-manager-service
          image: registry.gitlab.com/danielpinto8zz6/bikeshare/payment-manager-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9100
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: JVM_OPTS
              value: "-Xms1g -Xmx1g"
      imagePullSecrets: 
      - name: gitlab-auth
