kind: Service
apiVersion: v1
metadata:
  name: rent-service
  labels:
    app: rent-service
spec:
  selector:
    app: rent-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8500

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rent-service
  labels:
    app: rent-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rent-service
  template:
    metadata:
      labels:
        app: rent-service
    spec:
      containers:
        - name: rent-service
          image: registry.gitlab.com/danielpinto8zz6/bikeshare/rent-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8500
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: JVM_OPTS
              value: "-Xms1g -Xmx1g"
      imagePullSecrets: 
      - name: gitlab-auth
