kind: Service
apiVersion: v1
metadata:
  name: travel-history-service
  labels:
    app: travel-history-service
spec:
  selector:
    app: travel-history-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8600

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: travel-history-service
  labels:
    app: travel-history-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travel-history-service
  template:
    metadata:
      labels:
        app: travel-history-service
    spec:
      containers:
        - name: travel-history-service
          image: travel-history-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8600
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: JVM_OPTS
              value: "-Xms1g -Xmx1g"
#      imagePullSecrets: 
#      - name: gitlab-auth
