server:
  port: 8300
spring:
  profiles:
    active: "dev"
  application:
    name: feedback-service
  datasource:
    initialization-mode: always
    password: Pass2020!
    platform: postgres
    username: postgres
  jpa:
    generate-dll: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
  instance:
    preferIpAddress: true
    hostname: feedback-service

---

spring:
  profiles: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/feedback

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

---
spring:
  profiles: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/feedback

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

---
spring:
  profiles: prod
  datasource:
    url: jdbc:postgresql://postgres-feedback:5432/feedback

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka
  
